# AutoTax Production Environment
# Bu dosyayı .env olarak kopyalayın: cp env.example .env
# Üretimdeki değerleri MUTLAKA değiştirin!

# ── JWT Güvenlik ────────────────────────────────────────
# openssl rand -hex 32 komutuyla güvenli key üretin
JWT_SECRET=autotax_change_in_production_secret_key_2024
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=30

# ── Redis ───────────────────────────────────────────────
REDIS_PASSWORD=autotax_redis_pass

# ── CORS (virgülle ayırın) ──────────────────────────────
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000

# ── OCR Dilleri ─────────────────────────────────────────
OCR_LANG=deu+eng+fra+spa+ara+kor+chi_sim+tur
